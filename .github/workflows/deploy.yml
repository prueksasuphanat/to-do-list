name: Deploy to-do-list to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # 🔥 จุดทีเด็ดอยู่ตรงนี้ครับ!
      - name: Build project
        # สังเกตเครื่องหมาย -- (ขีดขีด) สองตัวตรงกลาง
        # มันบอกว่า "ข้อความข้างหลังนี้ ให้ส่งไปบอก Vite นะ"
        run: npm run build -- --base=/to-do-list/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' # Vite ปกติจะได้โฟลเดอร์นี้

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4